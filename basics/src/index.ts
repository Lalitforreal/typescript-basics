function greet(person : string) : string{
    return `hello ${person} how you doing`;
} 
// parameter type is person : string and the return type is string as give 

const username : string = "Lalit";
console.log(greet(username));